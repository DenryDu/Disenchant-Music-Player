<Page
    x:Class="DisenchantMusicPlayer.View.SongListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DisenchantMusicPlayer.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:localModel="using:DisenchantMusicPlayer.Model"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ListView x:Name="SonglistView" 
              ItemsSource="{x:Bind songListViewModel.SongList}" 
              SelectionChanged="{x:Bind songListViewModel.SonglistView_SelectionChanged}"
              >
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="localModel:MusicInfo">
                <Grid Height="80" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0"  Width="60" Height="60" CornerRadius="15">
                        <Image Source="{x:Bind Cover}" Stretch="UniformToFill"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{x:Bind Title}" Margin="8,0,0,0" TextWrapping="NoWrap" FontWeight="Bold" FontSize="15"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{x:Bind ArtistsStr}" Margin="8,0,0,0" TextWrapping="NoWrap"/>
                            <TextBlock>-</TextBlock>
                            <TextBlock Text="{x:Bind Album}" Margin="8,0,0,0" TextWrapping="NoWrap"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>
</Page>
